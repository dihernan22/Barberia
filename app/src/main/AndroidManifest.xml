<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.barberia">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".EditarCitaActivity"></activity>
        <activity android:name=".ListUserActivity" />
        <activity android:name=".ListCitasActivity" />
        <activity android:name=".RegistrarCitaActivity" />
        <activity android:name=".PrincipalActivity" />
        <activity android:name=".ListComboActivity" />
        <activity android:name=".ListBarbaActivity" />
        <activity android:name=".ListCabelloActivity" />
        <activity android:name=".RegistrarClienteActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>